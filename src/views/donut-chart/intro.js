import React from 'react';

export default () => {
  return (
    <div className="App-intro">
      <p>
        This example is a declarative port of the official D3{' '}
        <a href="https://bl.ocks.org/mbostock/3887193">Donut Chart</a> example.
        Please note that for simplicity, this example uses a pre-converted
        object array of data, rather than loading it via{' '}
        <code>d3.csv(...)</code>.
      </p>
      <p>
        Source:...<br />
        Blog:...
      </p>
    </div>
  );
};
